import type { FC } from "react";

export const StreamingCaret: FC = () => (
  <span
    className="streaming-caret inline-block w-0.5 bg-primary align-text-bottom shadow-[var(--glow)]"
    aria-hidden
  />
);
